{% extends "client/base.html.twig" %}

{% block contenu %}

<div class="main-card mb-3 card">
    <div class="card-body">
        <table style="width: 100%;" id="example" class="table table-hover table-striped table-bordered">
            <thead>
                <tr>
                    <th>Titre de l'UE</th>
                    <th>Connaissances</th>
                    <th>Compétences</th>
                </tr>
            </thead>
            <tbody>
                {% for ue in ues %}
                    <tr>
                        <td>{{ ue.titre }}</td>
                        <td>{{ ue.connaissance }}</td>
                        <td>{{ ue.competence }}</td>
                    </tr>
                {% endfor %}
            </tbody>
            <tfoot>
            </tfoot>
        </table>
    </div>
</div>

{% endblock %}
